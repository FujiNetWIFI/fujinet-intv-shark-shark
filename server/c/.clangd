# The Makefile passes -D_GNU_SOURCE (pthread_timedjoin_np, SOCK_NONBLOCK).
# Without it clangd reports those as implicit declarations even though the
# real build is clean at -Werror.
CompileFlags:
  Add: [-D_GNU_SOURCE, -std=c11, -Wall, -Wextra]
